// CONSECUTIVE THREE INTEGERS (1.02.25)(leetcode)
 /*#include<iostream>
#include<ctype.h>
using namespace std;
int main(){
    int flag = 0;
    int arrSize;
    cin >> arrSize;
    int arr[arrSize];
    for(int i=0;i<arrSize-1;i++){
        cin >> arr[i];
    }
    for(int i=0;i<arrSize;i++){
        if((arr[i] % 2 != 0) && (arr[i+1] % 2!=0) && (arr[i+2] %2!=0)){
            flag = 1;
            break;
        }
    }
    if(flag){
        cout << "true";
    }else{
        cout << "false";
    };
}*/